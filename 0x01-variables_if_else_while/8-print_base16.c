#include <stdio.h>

/**
* main - entry point.
*
* Return: Always 0.
*
*/

int main(void)
{

	int z;

	int c;

	for (z = 48; z <= 57; z++)
	{
		putchar(z);
	}

	for (c = 97; c <= 102; c++)
	{
		putchar(c);
	}

	putchar ('\n');

	return (0);
}
