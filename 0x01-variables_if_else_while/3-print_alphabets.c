#include <stdio.h>

/**
* main - entry point.
*
* Return: Always 0.
*
*/

int main(void)
{

	int z;

	int c;

	for (z = 97; z <= 122; z++)

	{
		putchar(z);
	}

	for (c = 65; c <= 90; c++)
	{
		putchar(c);
	}

	putchar('\n');

	return (0);
}
