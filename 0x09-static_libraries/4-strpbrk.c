#include "holberton.h"

/**
* *_strpbrk - entry point.
* @s: char pointer.
* @accept: char pointer.
* Return: 0.
*/

char *_strpbrk(char *s, char *accept)
{
	int i = 0;
	int p = 0;

	while (s[i] != 0)
	s++;
	while (accept[p] != 0)
	accept++;

	return (0);
}
